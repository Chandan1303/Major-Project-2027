import React, { useState } from 'react';
export default function PasswordInput({ label, error, register, name, placeholder = 'Enter your password', autoComplete = 'current-password' }) { const [visible, setVisible] = useState(false); return <div className="field"><label htmlFor={name}>{label}</label><div className="password-wrap"><input id={name} type={visible ? 'text' : 'password'} placeholder={placeholder} autoComplete={autoComplete} aria-invalid={!!error} {...register(name)} /><button type="button" className="reveal" onClick={() => setVisible(!visible)} aria-label={visible ? 'Hide password' : 'Show password'}>{visible ? 'Hide' : 'Show'}</button></div>{error && <p className="field-error" role="alert">{error.message}</p>}</div>; }
